/*
def fun(list,item,i=0,sum=0):
	if(sum==item):
		return True
	if(sum>item):
		return False
	if(i>=len(list)):
		return False
	return fun(list,item,i+1,sum) or fun(list,item,i+1,sum+list[i])


test=int(input())
for i in range(test):
	n,item=map(int,input().split())
	l=[]
	for i in range(n):
		x=int(input())
		l.append(x)
	if(fun(l,item)):
		print("Yes")
	else:
		print("No")
*/
#include<bits/stdc++.h>
using namespace std;

int main()
{


}
